= form_for(@friend) do |f|
  - if @friend.errors.any?
    #error_explanation
      %h2
        = pluralize(@friend.errors.count, "error")
        prohibited this friend from being saved:
      %ul
        - @friend.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :frequency_id
    %br/
    = select :friend, :frequency_id, Frequency.all.collect {|p| [ p.name, p.id ] }
  .actions
    = f.submit
